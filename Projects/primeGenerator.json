{"text": "<script src='/Projects/base/primes.js'></script>
  <script>
  function factorFromPage() {
    \tvar temp, divisor, num_list, x;
    \ttemp = document.getElementById('number').value;
    \tdivisor = 2;
    \tx = 0;
    \tnum_list = [];
    \twhile (divisor*divisor <= temp) {
      \t\twhile(temp%divisor == 0) {
        \t\t\tnum_list[x] = divisor;
        \t\t\ttemp /= divisor;
        \t\t\tx++;
      \t\t}
      \t\tdivisor++;
    \t}
    \tif(temp>1){ num_list[num_list.length] = temp; }
    \tdocument.getElementById('factorOutput').innerHTML = printList(num_list);
  }
  function printPrimesToPage() {
    var upTo = document.getElementById('numOfPrimes').value;
    if(upTo>10000){
      alert('Are you sure you want to do this?');
    }
  \tdocument.getElementById('primeOutput').innerHTML = printList(findPrimes(upTo));
  }</script>
  <h1>Primes and factoring</h1>
  <p>Enter a number to get factored:</p>
  <input id='number' type = 'text'>
  <button type='button' onclick='factorFromPage()'>Do it</button>
  <p id='factorOutput'></p><input id='numOfPrimes' type='text'>
  <button type='button' onclick='printPrimesToPage()'>Click to get first primes!</button>
  <p id='primeOutput'></p><br><br><a href='#' onclick='goto(\"Projects/index.json\")'>Back</a>", "title": "Prime Factoring", "header": "Primes"}
